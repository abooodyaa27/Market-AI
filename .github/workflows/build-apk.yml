name: Build Market AI Scalp AI V2.9 AI Decision APK

on:
  workflow_dispatch:
  pull_request:
    branches: [ ai-decision-v2.1 ]
  push:
    branches: [ ai-decision-v2.1 ]

concurrency:
  group: market-ai-scalp-ai-v29-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check JavaScript and analysis logic
        run: |
          for file in app/src/main/assets/*.js; do node --check "$file"; done
          npm test
      - name: Install browser test tools
        run: |
          npm install --ignore-scripts --no-audit --no-fund
          npx playwright install --with-deps chromium
      - name: Test mobile UI and BUY SELL WAIT flows
        run: npm run test:ui
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Retain test signing key
        uses: actions/cache@v4
        with:
          path: ~/.android/debug.keystore
          key: market-ai-scalp-ai-v28-debug-signing-v1
      - name: Build and lint Android
        run: gradle :app:assembleDebug :app:lintDebug --stacktrace
      - name: Verify APK
        run: |
          cp app/build/outputs/apk/debug/app-debug.apk Market_AI_SCALP_AI_V2_9_AI_Decision.apk
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose Market_AI_SCALP_AI_V2_9_AI_Decision.apk
          sha256sum Market_AI_SCALP_AI_V2_9_AI_Decision.apk > Market_AI_SCALP_AI_V2_9_AI_Decision.apk.sha256
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Market_AI_SCALP_AI_V2_9_AI_Decision.apk
          path: Market_AI_SCALP_AI_V2_9_AI_Decision.apk
          if-no-files-found: error
          retention-days: 90
      - name: Upload validation reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: SCALP_AI_V2_9-validation
          path: |
            test-results/
            app/build/reports/lint-results-debug.html
            Market_AI_SCALP_AI_V2_9_AI_Decision.apk.sha256
          if-no-files-found: ignore
          retention-days: 30
